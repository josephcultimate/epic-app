#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../karma-ember-preprocessor/bin/preprocessor" "$@"
  ret=$?
else 
  node  "$basedir/../karma-ember-preprocessor/bin/preprocessor" "$@"
  ret=$?
fi
exit $ret
